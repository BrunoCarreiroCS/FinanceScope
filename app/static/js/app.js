// FinanceScope - interacoes de interface.
// Por enquanto apenas placeholders; Chart.js e validacoes chegam nas Fases 3-6.

document.addEventListener("DOMContentLoaded", () => {
  // Marca a navegacao ativa via classe is-active (server-side ja define),
  // este hook fica reservado para realces dinamicos futuros.
  console.debug("FinanceScope UI pronta");
});
